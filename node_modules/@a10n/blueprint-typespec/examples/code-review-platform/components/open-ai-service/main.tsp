import "@a10n/blueprint-typespec";

using State;

@doc("Component representation of the OpenAI external service — reference this in @dependsOn and @testing instead of the bare dependency namespace.")
@component(#{
  type: ComponentType.Service,
  source: "https://platform.openai.com",
})
namespace Components.OpenAIService {}
