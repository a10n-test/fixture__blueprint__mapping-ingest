import "@a10n/blueprint-typespec";

using State;

@doc("Component representation of the GitHub external service — reference this in @dependsOn and @testing instead of the bare dependency namespace.")
@component(#{
  type: ComponentType.Service,
  source: "https://github.com",
})
namespace Components.GithubService {}
