import { DecoratorContext, Namespace, Operation, Program, UnionVariant } from "@typespec/compiler";
/**
 * @publishes(variant) — declares that this operation emits the given event variant.
 * May be applied multiple times (each call adds one entry to the list).
 */
export declare function $publishes(ctx: DecoratorContext, target: Operation, variant: unknown): void;
export declare function getPublishes(program: Program, op: Operation): UnionVariant[];
/**
 * @subscribes — marker decorator; input arg carries the event.
 * This decorator has NO arguments — it simply marks the op as a subscriber.
 * Must be placed inside a @worker namespace.
 */
export declare function $subscribes(ctx: DecoratorContext, target: Operation): void;
export declare function isSubscribesOp(program: Program, op: Operation): boolean;
/**
 * @worker — marks a namespace as a worker component.
 * Must be nested inside a @component namespace.
 */
export declare function $worker(ctx: DecoratorContext, target: Namespace, config?: unknown): void;
export declare function getWorkerConfig(program: Program, ns: Namespace): unknown;
export declare function isWorkerNamespace(program: Program, ns: Namespace): boolean;
//# sourceMappingURL=worker.d.ts.map