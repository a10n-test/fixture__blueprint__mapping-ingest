// Schema decorators removed — use native TypeSpec equivalents:
//   @closedModel  → @extension("additionalProperties", false)
//   @constValue   → @extension("const", value)
//   @uniqueItems  → TypeSpec.JsonSchema.@uniqueItems  (native)
//   @propExamples → multiple @example calls
//   @itemDoc      → typed scalar with @doc
//   @jsonRef      → use the actual TypeSpec type (e.g. dependant_id: Task.task_id)
//   @ref / @unique → live in typespec-dbml, not blueprint
export {};
